const useFormNewPropertyTypes = () => {
    const SET_IMAGE_PREVIEW_UPLOAD = "SET_IMAGE_PREVIEW_UPLOAD";
    return {
        SET_IMAGE_PREVIEW_UPLOAD,
    }
}

export default useFormNewPropertyTypes;